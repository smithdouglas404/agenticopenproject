# Agentic PPM stack (local / reference topology).
#
# Brings up: FalkorDB (the graph world-model AND the temporal layer — memory
# episodes + the append-only policy-version graph) + the agent-runtime sidecar.
# OpenProject is treated as EXTERNAL by default (point OPENPROJECT_BASE_URL at
# your Railway/hosted instance) — see the commented block at the bottom to run it
# locally too.
#
# On Railway: Railway deploys per-service, not via compose. Use this file as the
# source of truth and create one Railway service per block (see deploy/README.md).

services:
  falkordb:
    image: falkordb/falkordb:latest
    restart: unless-stopped
    ports:
      - "6379:6379"   # Redis protocol (graph queries)
      - "3001:3000"   # FalkorDB Browser UI
    volumes:
      - falkordata:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  agent-runtime:
    build:
      context: ..
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "8745:8745"
    environment:
      PORT: "8745"
      # OpenProject — point at your hosted/Railway instance:
      OPENPROJECT_BASE_URL: ${OPENPROJECT_BASE_URL:?set to your OpenProject URL}
      OPENPROJECT_API_KEY: ${OPENPROJECT_API_KEY:?set your OpenProject API key}
      OPENPROJECT_WEBHOOK_SECRET: ${OPENPROJECT_WEBHOOK_SECRET:-}
      OPENPROJECT_ALERTS_PROJECT: ${OPENPROJECT_ALERTS_PROJECT:-agent-alerts}
      AGENT_SYNC_SOURCE: ${AGENT_SYNC_SOURCE:-agentic-ppm}
      # Claude
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:?set your Anthropic API key}
      ANTHROPIC_MODEL: ${ANTHROPIC_MODEL:-claude-sonnet-4-6}
      # FalkorDB
      FALKORDB_HOST: falkordb
      FALKORDB_PORT: "6379"
      FALKORDB_GRAPH: ${FALKORDB_GRAPH:-agentic_ppm}
    depends_on:
      falkordb:
        condition: service_healthy

volumes:
  falkordata:

# ---------------------------------------------------------------------------
# OPTIONAL: run OpenProject locally too (all-in-one image). For real use prefer
# the maintained compose at https://github.com/opf/openproject-docker-compose.
# This repo's root docker-compose.yml is OpenProject *core-dev* only (guarded by
# LOCAL_DEV_CHECK) and is NOT for running an instance.
#
#  openproject:
#    image: openproject/openproject:15
#    restart: unless-stopped
#    ports:
#      - "8080:80"
#    environment:
#      OPENPROJECT_SECRET_KEY_BASE: ${OPENPROJECT_SECRET_KEY_BASE:?generate one}
#      OPENPROJECT_HOST__NAME: localhost:8080
#      OPENPROJECT_HTTPS: "false"
#    volumes:
#      - opdata:/var/openproject/assets
#      - opdb:/var/openproject/pgdata
#
# volumes (add): opdata: , opdb:
