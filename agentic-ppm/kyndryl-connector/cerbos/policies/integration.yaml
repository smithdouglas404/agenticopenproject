apiVersion: api.cerbos.dev/v1
resourcePolicy:
  version: "default"
  resource: "integration"
  rules:
    - actions: ["view"]
      effect: EFFECT_ALLOW
      roles: ["admin", "pmo"]
    - actions: ["connect", "configure", "delete"]
      effect: EFFECT_ALLOW
      roles: ["admin"]
