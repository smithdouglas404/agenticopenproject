<%= render Grids::WidgetBoxComponent.new(title: Setting.welcome_title.presence || organization_name, classes: "welcome") do %>
  <div class="wiki op-uc-container">
    <%= format_text(Setting.welcome_text, headings: false) %>
  </div>
<% end %>
