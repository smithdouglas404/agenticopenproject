<%= flex_layout(data: wrapper_data, classes: "users-non-working-days-calendar-view") do |calendar_page| %>
  <% calendar_page.with_row(classes: "op-fc-wrapper", data: { "users--non-working-days-target" => "calendar" }) %>
<% end %>
