<%= render(Primer::OpenProject::SubHeader.new) do |subheader|
      subheader.with_filter_button(
        icon_only: true,
        leading_icon: :search,
        mobile_label: t(:label_filter)
      )

      if render_create_button?
        subheader.with_action_button(
          tag: :a,
          href: create_path,
          scheme: :primary,
          leading_icon: :plus,
          label: create_label,
          data: { controller: "async-dialog" }
        ) { create_label }
      end
    end %>
