en:
  js:
    gitlab_integration:
      gitlab_pipelines: Pipelines
      tab_header_issue:
        title: "Issues"
      tab_header_mr:
        copy_menu:
          description: Copy git snippets to clipboard
          label: Git snippets
        create_mr:
          description: Create a Merge Request
          label: Create MR
        git_actions:
          branch_name: Branch name
          cmd: Create branch with empty commit
          commit_message: Commit message
          copy_error: ❌ Copy failed!
          copy_success: ✅ Copied!
          title: Quick snippets for Git
        title: "Merge requests"
      tab_issue:
        empty: >-
          There are no issues linked yet. Link an existing issue by using the code <code>OP#%{wp_id}</code> (or <code>PP#%{wp_id}</code>
          for private links) in the issue title/description or create a new issue.
      tab_mrs:
        empty: >-
          There are no merge requests linked yet. Link an existing MR by using the code <code>OP#%{wp_id}</code> (or <code>PP#%{wp_id}</code>
          for private links) in the MR title/description or create a new MR.
      updated_on: Updated on
      work_packages:
        tab_name: "GitLab"
    work_packages:
      tabs:
        gitlab: "GitLab"
