<%= helpers.angular_component_tag "opce-custom-modal-overlay" %>
<%=
  component_wrapper do
    primer_form_with(
      id: "project-section-edit-form",
      model: @project,
      method: :put,
      data: { turbo: true, turbo_stream: true, "test-selector": "async-dialog-content" },
      url: project_custom_field_section_path(project_id: @project.id, id: @project_custom_field_section.id)
    ) do |f|
      render(
        Projects::CustomFields::Form.new(
          f,
          project: @project,
          custom_field_section: @project_custom_field_section,
          wrapper_id: "#edit-project-custom-fields-dialog-#{@project_custom_field_section.id}"
        )
      )
    end
  end
%>
