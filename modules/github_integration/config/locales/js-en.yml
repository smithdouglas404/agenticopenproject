en:
  js:
    github_integration:
      github_actions: Actions
      pull_requests:
        merged_message: "Pull request #%{pr_number} %{pr_link} for %{repository_link} has been %{pr_state} by %{github_user_link}."
        message: "Pull request #%{pr_number} %{pr_link} for %{repository_link} authored by %{github_user_link} has been %{pr_state}."
        referenced_message: "Pull request #%{pr_number} %{pr_link} for %{repository_link} authored by %{github_user_link}
          referenced this work package."
        states:
          closed: 'closed'
          draft: 'drafted'
          merged: 'merged'
          opened: 'opened'
          ready_for_review: 'marked ready for review'
      tab_header:
        copy_menu:
          description: Copy git snippets to clipboard
          label: Git snippets
        git_actions:
          branch_name: Branch name
          cmd: Create branch with empty commit
          commit_message: Commit message
          copy_error: ❌ Copy failed!
          copy_success: ✅ Copied!
          title: Quick snippets for Git
        title: "Pull requests"
      tab_prs:
        empty: >-
          There are no pull requests linked yet. Link an existing PR by using the code <code>OP#%{wp_id}</code> in the PR
          description or create a new PR.


      work_packages:
        tab_name: "GitHub"
    work_packages:
      tabs:
        github: "GitHub"
