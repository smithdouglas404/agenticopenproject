<% html_title(t("my_page.label")) -%>

<%=
  render(Primer::OpenProject::PageHeader.new) do |header|
    header.with_title { t("my_page.label") }
    header.with_breadcrumbs(
      [
        t("my_page.label")
      ]
    )
  end
%>

<%= angular_component_tag "opce-my-page" %>
